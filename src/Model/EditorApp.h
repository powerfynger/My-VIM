#ifndef EDITORAPP_H
#define EDITORAPP_H

#include "../Utils/MyString.h"

class EditorApp
{
public:
    explicit EditorApp(const MyString &fileName);
    ~EditorApp();
    // EditorMode getMode();
    // void setMode(EditorMode mode);

private:
};

#endif